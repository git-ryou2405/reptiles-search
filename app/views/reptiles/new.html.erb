<% Rails.application.config.another_logger.debug "[d] view: reptiles/new id=#{current_user.id}" %>
<% provide(:button_text, '生体登録') %>
<% provide(:routes_url, user_reptiles_path(current_user)) %>

<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-9 col-sm-8 col-md-7 col-lg-5 col-xl-4 bgc-333 align-self-center">
      
      <!-- 生体登録 -->
      <%= render 'form', reptile: @reptile %>
    </div>
  </div>
</div>
