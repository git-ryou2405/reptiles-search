<% Rails.application.config.another_logger.debug "[d] view: reptiles/new" %>
<% provide(:button_text, '生体登録') %>
<% provide(:routes_url, user_reptiles_path(current_user) ) %>

<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-9 col-sm-8 col-md-7 col-lg-5 col-xl-4 align-self-center">

      <div class="row justify-content-center">
        <div class="col-11">
          <h2>生体登録</h2>
          <%= render 'form', reptile: @reptile %>

          <%= link_to '戻る', user_path(current_user) %>
        </div>
      </div>
      
    </div>
  </div>
</div>
